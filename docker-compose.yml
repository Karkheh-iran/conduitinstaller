services:
  conduit:
    image: ghcr.io/psiphon-labs/conduit:latest
    container_name: conduit
    restart: always
    env_file:
      - .env
    network_mode: host
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
